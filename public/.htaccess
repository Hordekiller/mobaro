RewriteEngine On

# Block direct access to uploaded files (videos, gallery uploads)
RewriteRule ^assets/uploads/ - [F,L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
